# Repository Instructions

## 时区与发布日期

- 本仓库的站点标准时区为 `Asia/Shanghai`（UTC+8）。`_config.yml` 必须保留 `timezone: Asia/Shanghai`，不得依赖构建服务器的本地时区。
- `_posts/` 下每篇文章的 frontmatter 必须显式声明 UTC 偏移，统一使用 `date: YYYY-MM-DD HH:mm:ss +0800`。禁止省略 `+0800`，因为 Jekyll 会把无偏移量的显式日期按 UTC 解析。
- 文件名中的 `YYYY-MM-DD` 必须与 frontmatter 日期换算到 `Asia/Shanghai` 后的自然日一致。
- 默认按立即发布处理时，文章日期不得晚于提交或自动任务执行时间。只有用户明确要求定时或未来发布时，才允许填写未来日期，并应在交付说明中注明计划发布时间。

## 文章标题与正文结构

- frontmatter 中的 `title` 是文章唯一的页面主标题。正文不得再次使用与其相同或近似的 `# 一级标题`。
- 正文应直接从导语开始；正文内的章节标题从 `## 二级标题` 开始。不得为了视觉重复而在导语前增加标题、副标题或同义改写的主标题。

## 内容标签

- 每篇文章最多使用 4 个 tags。在不损失检索和聚合价值的前提下，应尽量精简。
- 优先复用仓库已有 tags，避免为单篇文章随意创建新 tag。新 tag 必须表达可跨多篇内容复用的主题、领域、技术或内容类型。
- 避免使用复用价值较低的专有名称，包括公司名、个人名、产品名、项目名、活动名、股票代码和版本号。只有当某个专有名称已经形成长期栏目，或明确需要持续聚合多篇内容时，才可将其作为 tag。
- 避免使用同义、上下位重复或过于宽泛的 tags。新建 tag 使用小写英文；由多个单词组成时使用 kebab-case。

## 验证与发布

- 不强制在本地执行 Jekyll 构建，也不得仅为满足本仓库要求而额外安装 Ruby、Bundler 或其他构建工具。本地环境已经具备依赖时，可以运行完整构建作为发布前检查。
- 发布前必须完成与改动相称的静态检查，至少确认 frontmatter 完整、日期包含 `+0800`，且正文没有重复的一级标题。
- 对立即发布的文章，推送后必须确认 GitHub Pages 工作流成功、构建日志未出现目标文章被跳过或 `has a future date`，并确认目标 URL 返回成功状态且站点入口能够访问该文章。
- 对用户明确安排的未来文章，不要求在计划时间之前生成可访问页面；但必须确认发布日期、时区和文件名日期一致，并在计划时间之后验证 Pages 部署结果。
